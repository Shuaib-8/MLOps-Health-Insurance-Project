# Environment Variables for MLOps Health Insurance Project
# Copy this file to .env and adjust values for your local development environment

# =============================================================================
# LOCAL DEVELOPMENT CONFIGURATION
# =============================================================================
# For running applications locally (without Docker/Kubernetes)
# Usage: cp .env.example .env

# -----------------------------------------------------------------------------
# Streamlit Frontend Configuration
# -----------------------------------------------------------------------------
# API endpoint for Streamlit to connect to FastAPI backend
# Local development: http://localhost:8000
# Docker Compose: http://fastapi:8000
# Kubernetes: http://api:8000
API_ENDPOINT=http://localhost:8000

# API endpoint displayed to users in the UI (user-facing URL)
# This allows showing a clickable, accessible URL to users while using
# internal service names for actual API communication in containerized environments
# Local development: http://localhost:8000 (same as API_ENDPOINT)
# Docker Compose: http://localhost:8000 (user-accessible from host machine)
# Kubernetes: http://your-ingress-domain.com (public ingress URL)
API_ENDPOINT_DISPLAY=http://localhost:8000

# Application version (displayed in Streamlit footer)
APP_VERSION=2.0.0

# =============================================================================
# DEPLOYMENT ENVIRONMENTS
# =============================================================================
# NOTE: When deploying with Docker Compose or Kubernetes, these values are
# typically overridden by:
# - docker-compose.yml (environment section)
# - Kubernetes deployment manifests (env section)
# - Kubernetes ConfigMaps/Secrets
#
# This .env file is primarily for LOCAL DEVELOPMENT without containers.
# =============================================================================

# -----------------------------------------------------------------------------
# Optional: MLflow Configuration (if running locally)
# -----------------------------------------------------------------------------
# MLFLOW_TRACKING_URI=http://localhost:5555
# MLFLOW_BACKEND_STORE_URI=sqlite:///mlflow/mlflow.db
# MLFLOW_ARTIFACTS_DESTINATION=./mlflow/artifacts
